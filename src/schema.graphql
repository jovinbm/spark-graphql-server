type Book {
  id: Int!
  name: String!
  description: String!
  author: [Int!]!
  publisher: [Int!]!
  year: Int!
  url: String!
  genre: [Int!]!
}

type Author {
  id: Int!
  name: String!
  bio: String!
  url: String!
}

type Publisher {
  id: Int!
  name: String!
  url: String!
}

type Genre {
  id: Int!
  name: String!
}

type Query {
  books: [Book!]!
  authors: [Author!]!
  publishers: [Publisher!]!
  genres: [Genre!]!
}
